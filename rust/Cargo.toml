[workspace]
resolver = "2"
members = [
  "sdk",
  "examples/openai-chat-compatible",
  "examples/openai-image-compatible",
]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"
strip = true
