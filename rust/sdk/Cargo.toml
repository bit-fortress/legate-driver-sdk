[package]
name = "legate-driver-sdk"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Guest SDK for Legate Driver ABI v1"

[dependencies]
prost = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
